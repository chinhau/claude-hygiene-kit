# Capability watch — 2026-07-27

Window: 2026-06-27 → 2026-07-27 (first run; no previous report). Releases covered: 0.20.2 → 0.29.2.
Installed CLI: **0.29.1** (0.29.2, released today, is goal-mode fixes only).
Sources: [changelog](https://www.kimi.com/code/docs/en/kimi-code-cli/release-notes/changelog.html), [what's new](https://www.kimi.com/code/docs/en/kimi-code/whats-new.html).

Setup checked: `~/.kimi-code/AGENTS.md` (models block, dated 2026-07), `~/.kimi-code/config.toml`
(K3 default, four pinned `[models.*]` blocks, Stop gate hook), skills: capability-watch,
harness-audit, new-project. Launchd job runs `kimi -p`; nothing in the setup invokes `kimi server`.

## ACT

None. Nothing in the window breaks or invalidates the current setup: no config key in use was
renamed (`select_tools` → `dynamically_loaded_tools` is unused here), Stop-hook semantics are
unchanged, no pinned model was retired (K3/K2.7 current; K2.6 merely old), Extra Usage is an
additive opt-in rather than a quota shift, and the `kimi -p` changes (0.23.2 non-zero exit on turn
failure; 0.24.2 stays alive while background work is pending) are compatible improvements for the
launchd job, not breakage.

## CONSIDER

1. **Re-test `[secondary_model]` — 0.29.1 shipped "experimental secondary-model bindings for newly
   spawned subagents, including per-agent model preferences and subagent-only model overrides."**
   This is exactly the wiring the models block parked as "prose intent only; revisit each release."
   Removes the recurring task: keeping subagent cost down by orchestrator discipline instead of
   config. Test: point `[secondary_model]` at `moonshot-ai/kimi-k2.7-code`, spawn a subagent in the
   interactive TUI, check which model it actually runs. If honored → wire it in config.toml and
   delete the caveat line (see Reverse-drift). If still ignored → keep the line, re-check next
   release. Still labeled experimental; expect rough edges.

2. **Let the provider catalog carry the model list — API-key model-list auto-fetch (0.27.0) +
   `[models."<alias>".overrides]` (0.22.0).** Removes the recurring task: hand-editing the four
   `[models.*]` blocks in config.toml on every Moonshot catalog change (the "re-verify each major
   release" ritual). Proposal: confirm the auto-fetched catalog covers k2.7-code and
   k2.7-code-highspeed with correct capabilities; if so, delete those manual blocks, and convert
   K3's pinned metadata (`max_context_size = 1048576`, `support_efforts`, `default_effort`) into
   `[models."moonshot-ai/kimi-k3".overrides]`, which is documented to survive catalog refreshes.
   Verify before deleting — the catalog's advertised context for K3 may be the tier default, not 1M.

## IGNORE

- K3 launch and HighSpeed tier (what's-new) — already adopted: models block defaults to K3, both lighter tiers pinned.
- `/copy` slash command (0.27.0) — clipboard convenience; nothing to configure.
- Built-in `/check-kimi-code-docs` skill (0.24.2) — already present; docs Q&A, not a setup change.
- `[tools]` global enable/disable table (0.29.0) — no per-tool policy needed.
- Web-only features: SYSTEM.md override, Markdown-defined agents, attach-any-file, session export, design-system refresh, Mermaid (0.22.0–0.29.0) — setup is TUI + launchd, no web usage.
- Global default MCP timeouts (0.29.1) and MCP auto-reconnect (0.29.0) — no MCP servers configured.
- `KIMI_WEB_SEARCH_*` / `KIMI_WEB_FETCH_*` env vars (0.29.1) — OAuth login already works.
- `kimi server` deprecated in favor of foreground `kimi web` (0.28.0/0.28.1) — no `kimi server` usage in the run script or plist.
- Experimental on-demand tool loading, `select_tools` renamed `dynamically_loaded_tools` (0.23.0/0.24.2) — off by default, not enabled.
- Anthropic-compatible protocol + video input (0.20.2) — native `kimi` provider already covers this.
- `KIMI_CODE_CUSTOM_HEADERS` (0.20.2) — no custom-header need.
- Shell mode `!` (0.20.0) — interactive convenience; nothing to adopt in config.
- `kimi web --host` internet exposure (0.20.0) — not exposing the UI.
- Extra Usage / fuel pack (0.23.4; what's-new) — opt-in billing at kimi.com, not a config item.
- Bash timeout → auto-background (0.24.0) — good default; keeping it, opt-out key noted.
- Subagent timeout default 30 min → 2 h; `kimi -p` background/subagent timeouts removed (0.23.6/0.24.2) — suits the launchd job; nothing to set.
- Per-step LLM retry 3 → 10 (0.24.2) — default now absorbs transient 429/overload.
- Coder subagent parity with the main agent (0.26.0) — orchestrator working style benefits automatically.
- Reasoning kept across turns by default (0.23.0/0.23.1) — matches existing `[thinking] enabled = true`.
- Automatic oversized-image compression (0.22.0+) — default on; `[image]` knobs exist if ever needed.
- Goal-mode fixes across 0.23.x–0.29.2 (incl. today's step-limit and queued-message fixes) — no goal usage in this setup.
- Plugins can declare slash commands (0.21.0) — no plugins installed.
- Web security fixes: bearer-token bypass, workspace symlink escape (0.25.0) — already covered by installed 0.29.1.
- ACP sessions with non-OAuth credentials (0.28.1) — no ACP client in use.
- YOLO vs Auto description correction (0.28.0) — docs clarity only; behavior unchanged.

## Reverse-drift

- **`[secondary_model]` caveat in the models block** → candidate deletion once CONSIDER 1 confirms
  TUI support; the prose-intent line becomes a real config edit, as the block itself anticipated.
- **Manual `[models.*]` blocks** → candidate deletions once CONSIDER 2 confirms catalog coverage:
  the k2.7-code and k2.7-code-highspeed blocks are likely redundant post-0.27.0, and K3's block
  shrinks to an `overrides` table. Separately: the k2.6 block is pinned in config.toml but no
  longer referenced anywhere in AGENTS.md — superseded by K2.7/K3; propose deleting it regardless.
- **Stop gate hook (`gate.sh`)** — nothing in the window replaces it (the Interrupt hook event
  predates this window); keep.
- **launchd runner** — no drift: `kimi -p` only, and 0.23.2's non-zero exit on turn failure means
  failed runs now surface via exit status, which the old exit-0-on-failure behavior hid.

Counts: ACT 0 / CONSIDER 2 / IGNORE 25.
